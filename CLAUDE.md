# kydaidy-bot — прод-клон бота (Render auto-deploy с main)

> Управляющий слой и регламент — в главном репо `/Users/kai/kydaidy/CLAUDE.md`. Править код ЗДЕСЬ, деплой = push в main.
> **🔒 ОБЯЗАТЕЛЬНО прочитать `/Users/kai/kydaidy/ETALONS.md`** — закон проекта (эталоны/протокол ТЗ/запреты, мандат Кая 06.07). Брендовые тексты бота (persona/офферы/опенеры) = эталоны: менять только через tone-gate Алёны + ✅ Кая.

---

## 🚀 РЕЛИЗ-ПРОТОКОЛ + 🔐 БЕЗОПАСНОСТЬ (мандаты Кая 03.07, стандарт всех проектов)

**После КАЖДОГО нововведения/фикса (не в конце дня):** 1) обновить память проекта актуальной инфой; 2) DAY_LOG + ACTUALITY/TASKS/GOALS при смене статуса; 3) если менялся код — деплой (Render/CF/Railway) и проверка боем; 4) git commit + push в GitHub. Пропуск шага = задача не завершена.

**Безопасность:** секреты только в env/.gitignore (никогда в коде/коммитах/remote-URL/логах); перед пушем нового репо — скан секретов, история с секретами не пушится; засвеченный ключ = ротация; вебхуки с HMAC fail-closed; PAT не встраивать в remote; аудитор проверяет это в каждом QA. Полный стандарт: `~/projects-hub/ORG-STANDARD.md` §9–10.

<!-- PONYTAIL-START -->
## Ponytail: режим ленивого сеньора (анти-переусложнение)

**Лестница решений** (остановись на первой ступени, которая держит):
1. Это вообще надо строить? (YAGNI)
2. Это уже есть в кодовой базе?
3. Это делает стандартная библиотека?
4. Это покрывает нативная фича платформы?
5. Это решает уже установленная зависимость?
6. Можно ли в одну строку?
7. Только теперь: пиши минимум кода, который работает.

**Багфиксы:** отчёт называет симптом. Найди всех вызывающих функцию, которую трогаешь, и почини общую функцию один раз — один guard там меньше, чем по одному на каждый вызов. Лечи причину, не симптом.

**Правила:**
- Никаких незапрошенных абстракций, зависимостей, boilerplate
- Удаление важнее добавления; скучное важнее умного; минимум файлов
- Побеждает кратчайший рабочий diff — но только после понимания задачи
- Оспаривай сложные запросы; при равном размере корректность к краевым случаям важнее «умного»
- Помечай намеренные упрощения комментарием `ponytail:` — что упростил и куда смотреть, если усложнять

**Не режется никогда:**
- Полное понимание задачи до кода
- Валидация ввода на границах доверия
- Обработка ошибок, предотвращающая потерю данных
- Безопасность, доступность
- Всё, что явно запрошено
- Нетривиальная логика требует ОДНОЙ запускаемой проверки (assert/маленький тест)
<!-- PONYTAIL-END -->

<!-- FILE-PLACEMENT-REF-START -->
## 📁 Размещение файлов
Действует глобальный закон `~/.claude/CLAUDE.md` → «РАЗМЕЩЕНИЕ ФАЙЛОВ» (мандат 28.07.2026): любой созданный/скачанный файл сразу в папку проекта (`docs/`, `research/`, контент-папка для тяжёлого медиа, `credentials/` под .gitignore для секретов). На рабочий стол — только по явной просьбе Кая. Неопознанное — в `~/Documents/desktop-inbox-<дата>/`.
<!-- FILE-PLACEMENT-REF-END -->

---

## 🧭 РЕГЛАМЕНТ КОМПЕТЕНТНОСТИ (мандат Кая 15.08.2026) — ОБЯЗАТЕЛЕН

Канон целиком: `~/.claude/CLAUDE.md` → «РЕГЛАМЕНТ КОМПЕТЕНТНОСТИ». Читается при выполнении
КАЖДОЙ задачи наравне с «ПРОТОКОЛОМ ЗАДАЧИ». Кратко, десять правил:

1. **Уверенность до действия** — знать точно, как выполняется задача, по актуальным на сегодня
   нормативам, инструкциям и документации. «Примерно помню» = неверное действие.
2. **Перепроверка первоисточником** — всё полученное (от органа, поставщика, агента, своей
   памяти) сверяется с источником до действия, а не после.
3. **Факты с аргументами** — ссылка на действующий документ и дата проверки. «Кажется» запрещено.
4. **Фиксировать все детали** — сноски, примечания, ограничения. Незаписанное не существует.
5. **Письменное ТЗ на каждую задачу** — себе и агентам: вход, выход, чем подтверждено,
   критерий готовности, что может сломаться.
6. **Инструмент только с пруфом** — где сработал, почему сработал, чем подтверждено.
   Не понял механизм — не применяй.
7. **Каждое действие — воспроизводимый навык**: шаги, точки отказа, признаки успеха.
   Под повтор, передачу агенту, автоматизацию и монетизацию.
8. **Не пропускать задачи**, не делать «частично», не ломать работающее без аргументации.
9. **Одна задача за раз**; параллельно — только непересекающееся; делегировать только
   агентам с прописанными инструкциями, скриптами, скилами.
10. **Делать самому** — не можешь, узнай как и сделай. Кай зовётся в крайнем случае.

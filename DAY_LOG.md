# DAY_LOG — kydaidy-bot

## 2026-07-10 — Перестройка воронки (лестница Ханта + квалификация)

Планёрка (восстановлена из оборванной сессии): корень продаж=0 = продаём холодным,
ступеней Ханта 2-3 нет, гейт меряет катарсис. Мастер-ТЗ: `docs/funnel-rebuild-TZ-2026-07-10.md`.

### ✅ Фаза I (изолированное, задеплоено) — commit e78e5e8
- **Блок 1 (peak-сигнал-фикс):** `stale_nudge_peak_minutes` 7→35. На пике НЕ продаём по
  таймеру (`_PEAK_HOLD` присутствие-страховка) — оффер триггерит сообщение клиентки.
  Симметричная проверка порога. Чинит баг #32 (продажа за 80с до «а куда дальше»).
- **Блок 5а:** 3 канал-нуджа разведены (предвкушение / momentum / бесплатный голос).
- **Блок 5б:** фрагмент эфира `club_demo_file_id` в Touch2 (соц-пруф, крэш-сейф, пустой→как было).
- **Блок 6:** превью-сетка 28 боевых + 21 карантин роликов → Кай отбраковывает (Artifact).
  Кай: автозалив без глаза — нет; сначала превью.

### ✅ Фаза II (оффер-путь, ЗА ФЛАГАМИ OFF — код собран, ждёт бой) — задеплоено
- **S5 (73d92ef):** гейт-инфра `purchase_stage` (модуль purchase_stage.py, колонки,
  fail-open `stage_allows_offer`, `refresh` после хода/подписки/клика). compute_stage
  юнит-тест 8/8. Флаги OFF.
- **S6+S7 (15386be):** Блок 2 директива-сравнение в give_shift (hunt_stage2) + событие
  hunt_comparison_shown; Блок 3 выбор двери по ТЕМПЕРАТУРЕ (hunt_stage3): холодная→Клуб,
  1:1 только hot+жёсткая одиночка R/Q/H/C; событие product_choice_shown.
- **Блок 4 гейт-обвязка:** `_do_after_close` (не квалифицирована → тёплое присутствие
  без цены, followup не ставим) + stale-нудж не-пик за гейтом. dead_session покрыт.
- Черновик текстов: `docs/funnel-texts-draft-2026-07-10.md` → тон-гейт + глаз Алёны.

### 🔴 ЖДЁТ КАЯ (S8 — включение с боем D1)
1. Отбраковка 42 роликов (Artifact-сетка) → залив годного.
2. Тон-гейт + ✅ Алёны на тексты ступеней 2-3.
3. Включать флаги ПО ОДНОМУ в Render env + бой D1 после каждого:
   `PURCHASE_STAGE_GATE_ENABLED` → `HUNT_STAGE2_ENABLED` → `HUNT_STAGE3_ENABLED`
   → `COMPARISON_STEP_REQUIRED` (последним, когда stage2 в проде).

### 🟢 S8 шаг 1 ВКЛЮЧЁН (10.07, с сервера kai-tokyo, Кай дал ход «делаем по плану»)
- `PURCHASE_STAGE_GATE_ENABLED=true` выставлен через Render API, деплой
  `dep-d98b8jmtrd3s73ec3aeg` live на коммите `b728197`.
- Бой (пассивный): `/health` = `ok b728197`; логи чистые (TelegramConflictError
  только 20-60с окна деплоя — известный артефакт polling); D1: колонки
  `users.purchase_stage/_at` на месте, значения пустые (пишутся после первого
  хода — трафика после включения не было).
- Fail-open подтверждён перечиткой `purchase_stage.py:103-129`: флаг ON, но
  stage=None → allow + событие `gate_unknown_stage_allowed`. Whitelist Кай
  (6271776494, tg_admin_id) + Алёна (680319075).
- ⏭ Флаги 2-4 НЕ включаю: `HUNT_STAGE2/3` заблокированы п.2 (тексты ступеней
  ждут тон-гейт + ✅ Алёны), плюс нужен активный бой D1 — ход Кая в боте, чтобы
  увидеть запись стадии и событие гейта.

### 📤 Согласование выкладки 10.07 отправлено Каю (ТГ msg 1539-1542)
- Кандидаты дня: Дикая-PIP + Эмма-PIP (канал 1, темп 2/день), Громов Р2 (канал 2, опция).
- Каждое видео с готовыми названием/описанием/тегами по ФОРМУЛА-НАЗВАНИЙ; сводка с 🟢/🔴.
- Вопросы в сводке: Билл PIP vs ТЕСТ-B (один на фильм), ПЕТЛИ в архив, Р1 переназвать хук.
- Скрипт: kydaidy/scripts/_send_approval_2026-07-10.py. Остаток очереди ~5 дней контента.

## 2026-07-11 — S3 добит: Touch2-эфир оживлён (сервер)
- Код Блока 5б был в проде с 10.07, но env `CLUB_DEMO_FILE_ID` не был задан →
  followup_2 сегодня 01:38 UTC ушёл юзеру БЕЗ эфира (крэш-сейф отработал «как было»).
- Через Render API поставлен `CLUB_DEMO_FILE_ID` = file_id стрима v8 (тот же ассет,
  что одобрен как соц-пруф `socproof_video_file_id` в bot_meta). Деплой
  dep-d9931pnaqgkc73flmj5g live, /health = ok ea23846. Rollback = очистить env.
- Гейт (пассивный бой): purchase_stage в D1 пуст — трафика после включения флага
  не было. Флаги HUNT_STAGE2/3 + COMPARISON_STEP_REQUIRED не трогал (ждут тексты).

## 2026-07-12 — Тексты ступеней v2: нейромеханизм (мандат Кая)
- Все тексты воронки переписаны с нейропсихологией простыми словами (мозг/дорожка/
  нервная система, без терминов): директива ступени 2 (4 пути + нейро-объяснение
  каждого), шаблоны двери ступени 3, гейт-warm, peak-hold. Commit 9b6a40f, live.
- peak-hold и гейт-warm УЖЕ боевые (Фаза I + гейт вкл). Ступени 2-3 — за флагами.
- Черновик v2: docs/funnel-texts-draft-2026-07-10.md → ждёт глаз Алёны → флаги.

## 2026-07-13 — S8 шаги 2-3: ступени Ханта ВКЛЮЧЕНЫ (Кай: «включай»)
- `HUNT_STAGE2_ENABLED=true` + `HUNT_STAGE3_ENABLED=true` через Render API,
  деплой dep-d9a79vvaqgkc7397a2vg live, /health = ok df98120.
- Env сверен: PURCHASE_STAGE_GATE + HUNT_STAGE2 + HUNT_STAGE3 = true,
  CLUB_DEMO_FILE_ID задан. COMPARISON_STEP_REQUIRED — НЕ включён (по ТЗ последним,
  после боя stage2: нужно увидеть hunt_comparison_shown в D1 на реальной встрече).
- Теперь живая воронка: give_shift несёт нейро-сравнение путей → выбор двери по
  температуре (холодная→Клуб, 1:1 только hot+одиночка) → цена. Гейт греет
  неквалифицированных без цены.
- ⏭ Бой: нужна реальная встреча (Кай /alena или юзер) → проверить D1:
  purchase_stage записан, hunt_comparison_shown, product_choice_shown, порядок
  событий, оффер выстрелил ≤ TURN_CAP. После боя → COMPARISON_STEP_REQUIRED=true.

## 2026-08-29 — Мёртвая воронка «Манифест» убрана из живых путей бота

Повод: Кай получил в @kydaidy_bot «Карту перепутья» и воркбук «Манифест 7».
Мандат Кая 28.08 — старая воронка мертва целиком, значит любое живое упоминание
это дефект. Источник правды по живой воронке: `~/kydaidy-alyona/docs/VORONKA-2026-08-28.md`.

### Достижимое человеком — убрано (commit 26a7b82)
- **bot.py:** сняты с регистрации `alena_router` (AI-встреча, закрывавшая на Клуб
  990 ₽), `guide_router` (проводник по воркбуку, /praktiki), `book_router`
  (/zapis, «Манифест 1:1»), `growth_router` (реактивация в Клуб), `curator_router`
  (батч контента по тесту Тени и колоде «Карта перепутья»). Сняты семь тиков-
  отправителей: stale / reengage / orphan / dead / club_ladder, growth,
  push_daily_batch + publish_tick. Кнопка меню «Дом семьи» → «Открыть приложение».
- **handlers.py (818 строк долой):** карта перепутья с PDF и аудио
  (`/start povorotN`), «Ты на Повороте N» на `/start`, витрина `/products` + `buy:*`,
  `/club`, `/cabinet`, `/quiz` и весь путь теста Тени (фото → портрет → AI-встреча),
  захват постов Tribute (`/capture`, `cap:*`, `auto_capture_tribute`), `/stop`.
  Оффер Клуба убран из «Я уже в канале» (`check_sub`) и из catch-all fallback,
  `/help` переписан на живые команды. Главное меню = две двери новой воронки:
  «Какой у нас сценарий» (`para:vhod`) и «Записаться на разбор» (`razbor_start`).
- **webhooks.py:** снят эндпоинт `/webhook/tally` (форма квиза «5 поворотов»,
  она и писала людям povorot в базу) и маршрут `/p/{token}` (портрет Тени).
- **content_data.py:** вычищен до одного живого текста `WELCOME_NO_POVOROT`.

### Мёртвый код — убран отдельно (commit 4540809)
`nurture.py`, `manifest7_guide.py`, `manifest7_guide_data.py`, `booking.py`,
`followup.py`, `portrait_store.py` — после снятия роутеров их не импортирует никто.

### Оставлено сознательно
- Распознавание СТАРЫХ оплат в `webhooks._tribute_product_code` — продления
  подписок ещё приходят, платившим доступ выдаётся. Маркетинг закрытой воронки
  из текстов после оплаты убран, ссылки на снятые команды тоже.
- `alena_chat.py` + `alena_persona/brain/voice/knowledge`, `curator.py`,
  `growth_agent.py`, `shadow_test.py`, `ai_quiz.py` — код в репозитории, но
  ни одной строкой не достижим из чата. Удаление = решение Кая (это целые
  продукты, а не остатки), плюс на них держатся test_funnel / test_e2e_path / test_brain.

### Прибор
`proverka_voronki.py` — две проверки в одном файле: (1) слов мёртвой воронки нет
в текстах достижимых модулей, (2) у каждой кнопки достижимого экрана есть
обработчик. Достижимость считается от `bot.py` по `include_router` / `add_job`
транзитивно; докстринги не в счёт; оговорки только явные — `# voronka: ok — причина`
(сейчас одна, в webhooks). Обе проверки доказали, что умеют краснеть.

### Чем проверено
| Что | Результат |
|---|---|
| `proverka_voronki.py` до правки | КРАСНЫЙ, 112 попаданий в 12 модулях |
| `proverka_voronki.py` после | ЗЕЛЁНЫЙ обе проверки, достижимых модулей 20 (было 40) |
| Канарейка прибора (вставленный дефект в razbor.py) | покраснел на обеих проверках |
| test_funnel / test_e2e_path / test_lead_policy / test_brain / test_bank | 5/5 OK |
| pyflakes по изменённым файлам | чисто |
| `import bot` | OK |

### Осталось Каю
1. **Деактивировать витрины в Tribute** (Клуб «Манифест», «Манифест 1:1»,
   «Манифест 7»): ссылки живут вне бота, купить пока можно.
2. **Судьба AI-Алёны** (`/alena`, встреча + голос + кружки): весь её продающий
   слой был построен на Клубе 990 ₽, нового текста в источнике правды нет —
   поэтому путь снят целиком, а не переписан. Оживлять под разбор или удалять?
3. **Слой «Атмосфера дома» / «6 секунд» / чек-ин** остался включённым: в §12
   источника правды он назван мёртвым, но слов «Манифеста» не содержит и в это
   ТЗ не входил. Тики `run_atm_nextday_tick`, `run_sixsec_tick`, `run_checkin_tick`
   ещё шлют сообщения.
4. **48 МБ мёртвых аудио** в `content/audio` (povorot-1..5, manifest-day,
   manifest-7) — кода, который их читает, больше нет.

---

## 29.08.2026 (продолжение) — старое приложение снесено: атмосфера, вечера, чек-ин, мёртвые аудио

**Повод.** Слово Кая по остатку первого захода: «по всем остальным пунктам всё
сносить, у нас есть только новая воронка, старую полностью удаляем, все каналы,
всё-всё-всё». «Каналы» уточнены как пути и слои внутри бота — Telegram-канал
`t.me/kydaidy` не трогали ничем.

**Доказательство мёртвости** — `~/kydaidy-alyona/docs/VORONKA-2026-08-28.md` §12
«Что умерло 28.08», строка дословно: «Старое приложение: тест атмосферы, вечера,
чек-ин, банк 5:1, витрины клуба». Слой снесён по этой строке, не по пересказу.

### Что снято
| Путь | Чем был | Как снят |
|---|---|---|
| `atm_router` (/dom, `atmq:*`) | тест «Атмосфера дома», 12 вопросов | роутер снят, `quiz_atmosfera.py` + `quiz_atmosfera_data.py` удалены |
| `sixsec_router` (`six:*`) | «6 секунд», три вечера тепла | роутер снят, `sixsec.py` + `sixsec_data.py` удалены |
| `checkin_router` (/checkin, `chk:*`) | дневной чек-ин + парный gate банка 5:1 | роутер снят, `checkin.py` удалён |
| `run_atm_nextday_tick` (30 мин) | next-day чек после теста | тик снят |
| `run_sixsec_tick` (30 мин) | вечера 2–3 | тик снят |
| `run_checkin_tick` (cron 21:00 МСК) | ежедневный вопрос обоим в паре | тик снят |
| `?start=pair_<uid>` | вёл приглашённого партнёра в экран `#/test` старого приложения | ветка снята, ссылка не падает — человек попадает на общий вход новой воронки |
| `week_data.py` | платная неделя вечеров, импортировала `sixsec_data` | удалён (вызывающих не было) |
| `content/audio` 48 МБ | povorot-1..5, letter-1, manifest-day, manifest-7 | читающего кода нет (грепнуто); отслеживаемые — `git rm`, `manifest-7` (34 МБ, был под .gitignore, единственная копия) перенесён в `~/kydaidy-alyona-content/_arhiv-audio-manifest-2026-08-29/` |
| `database.py` | 13 функций-сирот снятого слоя | удалены `checkin_*` (5), `sixsec_*` (4), `couples_full`, `couple_partner`, `atm_get_result`, `atm_nextday_due`, `atm_mark_nextday` |

**Вес в git-истории не уходит.** Удалённые mp3 остаются в объектах прошлых
коммитов: `.git` как был ~50 МБ, так и остался. Историю не переписывали —
переписать её можно только форс-пушем поверх живого прода.

### Что оставлено и почему
- **AI-Алёна** (`alena_chat/persona/brain/voice/knowledge`, `brain_cascade`) —
  прямое изъятие Кая: не трогать, возможно переедет в новый проект. Роутер снят
  прошлым заходом, человеку недостижима, ни строки не изменено.
- **Таблицы `atm_quiz`, `sixsec`, `checkin_pause`, `checkin_ledger`, `bank_ledger`,
  `couples`** — схему не дропали. Причина: в них могут лежать данные живых людей,
  а миграция на удаление даёт ноль пользы (прод на Render free, БД `./kydaidy.db`).
  Кода, который в них пишет из чата, больше нет.
- **Банк 5:1 в `database.py`** (`resolve_couple`, `bank_add`, `get_bank`,
  `merge_banks`, `render_bank_card`, `atm_save_result`) — в §12 назван мёртвым,
  но на нём стоит `test_bank.py` из критерия приёмки. Из чата недостижим:
  единственные двери (чек-ин и вечера) снесены.
- **`config.py` / `render.yaml`: `MANIFEST_*_PRICE`, id закрытых каналов** —
  их читает разбор входящих платежей Tribute, а Tribute закрывает Кай сам.

### Прибор
`proverka_voronki.py` дорос до четырёх проверок: (1) слов мёртвой воронки нет в
достижимых текстах, (2) у каждой кнопки есть обработчик, (3) **мёртвый слой не
вернулся ни роутером, ни тиком планировщика** — тик называется отдельно, потому
что пишет живым людям сам, по таймеру, (4) **пути новой воронки на месте**
(`quiz_para`, `razbor`, `dnevnik`, `drip_para`) — уборка не имеет права унести
живое. Отсутствие тиков в `bot.py` = красный, а не «проверять нечего».

### Как краснела канарейка
1. Внутренняя, до вердикта: вердикт гоняется на подложенном наборе
   (`quiz_atmosfera` в роутерах, `checkin` в тиках) — обязан назвать оба, причём
   тик именно тиком; обратная проверка — на чистом входе обязан молчать; разбор
   `add_job` пробуется на исходнике с `run_growth_tick`.
2. Сквозная, на живом `bot.py`: в реальный файл вписаны
   `dp.include_router(growth_router)` и `scheduler.add_job(run_growth_tick, …)`.
   Прибор: `КРАСНЫЙ: мёртвый слой вернулся в живые пути — 2` (`growth_agent` —
   ТИК, `shadow_test` — транзитивно через импорт), плюс 4 слова «Манифеста» в
   текстах. `bot.py` восстановлен из копии, md5 сверен до и после.

### Чем проверено
| Что | Результат |
|---|---|
| `proverka_voronki.py` | ЗЕЛЁНЫЙ 4/4, достижимых модулей 15 (было 20) |
| Сквозная канарейка (вернули `growth_router` + тик в живой `bot.py`) | КРАСНЫЙ, rc=1, назвал тик тиком |
| `test_funnel` / `test_e2e_path` / `test_lead_policy` / `test_brain` / `test_bank` | 5/5 OK (до и после правки) |
| `proverka_slota.py` | слот бота и приложения совпадают, 144 проверки |
| `pyflakes` по изменённым файлам | чисто |
| `import bot` | OK |

### Осталось Каю
1. **Tribute** — деактивировать витрины «Манифеста» (закрывает сам).
2. **Mini App `https://kydaidy.com/app/`** — живёт в `~/kydaidy-alyona`, не в этом
   репозитории. Синяя кнопка меню бота открывает его корень. Экран `#/test` там
   всё ещё может показывать тест «Атмосфера дома» — из бота ссылок на него
   больше нет, но само приложение проверяет другой исполнитель.
3. **Судьба AI-Алёны** — код цел и не трогался, ждёт решения.
